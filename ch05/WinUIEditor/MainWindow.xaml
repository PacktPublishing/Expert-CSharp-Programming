<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="WinUIEditor.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUIEditor"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="WinUI Editor">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid x:Name="Root" RowDefinitions="Auto,*,Auto" Padding="12">
        <controls:CommandBar DefaultLabelPosition="Bottom">
            <AppBarButton Label="New" Command="{Binding NewCommand}">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Page2" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Open" Command="{Binding OpenCommand}">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="OpenFile" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Save" Command="{Binding SaveCommand}">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Save" />
                </AppBarButton.Icon>
            </AppBarButton>
            <AppBarButton Label="Save As" Command="{Binding SaveAsCommand}">
                <AppBarButton.Icon>
                    <SymbolIcon Symbol="Save" />
                </AppBarButton.Icon>
            </AppBarButton>
        </controls:CommandBar>

        <TextBox Grid.Row="1"
                 Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 TextWrapping="Wrap"
                 AcceptsReturn="True"
                 VerticalAlignment="Stretch"
                 HorizontalAlignment="Stretch"
                 FontFamily="Consolas"
                 FontSize="14"
                 ScrollViewer.VerticalScrollBarVisibility="Auto" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12">
            <TextBlock Text="File:"/>
            <TextBlock Text="{Binding FileName}" FontWeight="SemiBold"/>
            <TextBlock Text="  "/>
            <TextBlock Text="{Binding Status}" Foreground="Gray"/>
        </StackPanel>
    </Grid>
</Window>
